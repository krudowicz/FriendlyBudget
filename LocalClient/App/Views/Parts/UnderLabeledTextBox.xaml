<UserControl x:Class="FriendlyBudget.LocalClient.App.Views.Parts.UnderLabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FriendlyBudget.LocalClient.App.Views.Parts"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Height="20" MinWidth="100" Width="auto"  MaxWidth="200" Foreground="#ff9800" Background="#2a2a2a" BorderThickness="1" BorderBrush="#ff9800" VerticalAlignment="Bottom"></TextBox>
        <Label Grid.Row="1" Foreground="#ff9800" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="10" Content="{Binding LocalLabel}"></Label>
    </Grid>
</UserControl>
